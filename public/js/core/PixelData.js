define([], function(){
	var that = {};
	// returns new BitmapData object containing pixels a - b
	that.difference = function(a, b) {
		
	};

	// returns an array of polygons which roughly approximate the pixels
	that.polygonify = function(bmp) {

	};
	return that;
});